import { RangeDropdownDirective } from "./range-dropdown.directive";

describe("RangeDropdownDirective", () => {
	it("should create an instance", () => {
		const directive = new RangeDropdownDirective();
		expect(directive).toBeTruthy();
	});
});
